{
  "name": "yodha-data-eng-agent",
  "displayName": "Yodha Data Engineer",
  "description": "AI data engineer: Airflow, PySpark, dbt, Kafka, Delta Lake",
  "version": "0.1.0",
  "publisher": "yodha",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "keywords": [
    "ai",
    "agent",
    "yodha",
    "data-eng"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "capabilities": {
    "languageModels": "true"
  },
  "contributes": {
    "chatParticipants": [
      {
        "id": "yodha.data-eng",
        "name": "yodha-data",
        "fullName": "Yodha Data Engineer",
        "description": "AI data engineer: Airflow, PySpark, dbt, Kafka, Delta Lake",
        "isSticky": false,
        "commands": [
          {
            "name": "help",
            "description": "Show Data Engineer capabilities"
          }
        ]
      }
    ],
    "configuration": {
      "title": "Yodha Data Engineer",
      "properties": {
        "yodha-data-eng.aiProvider": {
          "type": "string",
          "enum": [
            "vscode",
            "claude",
            "openai"
          ],
          "enumDescriptions": [
            "VS Code built-in LLM via GitHub Copilot (no API key needed)",
            "Anthropic Claude (requires claudeApiKey)",
            "OpenAI GPT-4 (requires openaiApiKey)"
          ],
          "default": "vscode",
          "description": "AI provider for this agent"
        },
        "yodha-data-eng.claudeApiKey": {
          "type": "string",
          "default": "",
          "description": "Anthropic Claude API key (sk-ant-...)"
        },
        "yodha-data-eng.openaiApiKey": {
          "type": "string",
          "default": "",
          "description": "OpenAI API key (sk-...)"
        },
        "yodha-data-eng.claudeModel": {
          "type": "string",
          "default": "claude-sonnet-4-5",
          "description": "Claude model to use"
        },
        "yodha-data-eng.openaiModel": {
          "type": "string",
          "default": "gpt-4o",
          "description": "OpenAI model to use"
        },
        "yodha-data-eng.vscodeModel": {
          "type": "string",
          "default": "gpt-4o",
          "description": "VS Code LLM model family (e.g. gpt-4o, gpt-4o-mini)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}